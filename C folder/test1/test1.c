#include <stdio.h> 
#include <stdlib.h>
#include <ctype.h>
  
int main() { 
    // system("chcp 65001");   
    // system("cls");
    float height, base, square;
    char h = '8';
    printf("%c", isdigit(h));
    // printf("Уведіть висоту:");
    // scanf("%f", &height);
    // printf("%f", height);
    // printf("\nEnter the height: ");
    // scanf("%f", &height);
    // square = base * height *0.5;
    // if (square <= 0)
    // {
    //     printf("\nWrong format.");
    // }
    // else
    // {   printf("\nWith base: %.3f and height: %.03f the square: %f", base, height, square);
    // }
    getchar();
    return 0;
} 